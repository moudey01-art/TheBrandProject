# TheBrandProject

Monorepo starter for brand guides, site, and app.

Commands:

- `npm install` — installs dependencies for the whole workspace
- `npm run build` — run build placeholder

Structure:

- packages/brand-guides — brand assets and guidelines
- packages/site — public website
- packages/app — application code
